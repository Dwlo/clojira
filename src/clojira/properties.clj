(ns clojira.properties)

;; Rest call
(def jira-server-url   "...")
(def jira-user-name    "...")
(def jira-user-passwd  "...")
;; E-Mail
;;       -- smtp config
(def smtp-host         "...")
(def email-address     "...")
(def email-password    "...")
;;       -- mail content
(def user-email        "...")
(def user-name         "...")
(def robot             "Lisp Alien")
;; Export dir
(def export-output-dir "...")
